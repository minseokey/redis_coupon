
events {
    worker_connections 1024;
}

http {
    upstream coupon {
        server application1:8081;
        server application2:8082;
        server application3:8083;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://coupon;
        }
    }
}
